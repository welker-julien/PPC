price(prix_courant,tab_ext,temperature,totaux):
	def ext(tab,coef):
		for i in range (0,len(tab)):
			tot=tab[i]*coef
		return tot

	def int(temp,tot):
		for i in range (1,len(sys.argv):
			tot=i*argv[i]

	return 0,99*prix_courant+ext(tab_ext,0.001)+int(0.01,temperature,totaux)
